[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "bridge-app"
version = "0.1.0"

[tool.setuptools]
packages = ["config", "src", "src.parser"]
